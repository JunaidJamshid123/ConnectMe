<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />
    
    <!-- Required permissions -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    
    <!-- Optional permissions -->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN" />

    <application
        android:name=".ConnectMeApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.I211203"
        tools:targetApi="31">

        <!-- Splash Screen (Launcher) -->
        <activity
            android:name=".SplashScreen"
            android:exported="true"
            android:theme="@style/Theme.I211203">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Clean Architecture Presentation Layer Activities -->
        
        <!-- Main Activity with Bottom Navigation -->
        <activity
            android:name=".presentation.main.MainActivityNew"
            android:exported="false"
            android:theme="@style/Theme.I211203" />

        <!-- Auth Activities -->
        <activity
            android:name=".presentation.auth.LoginActivity"
            android:exported="false"
            android:theme="@style/Theme.I211203" />

        <activity
            android:name=".presentation.auth.SignUpActivity"
            android:exported="false"
            android:theme="@style/Theme.I211203" />

        <!-- Chat Activity -->
        <activity
            android:name=".presentation.chat.ChatActivity"
            android:exported="false"
            android:theme="@style/Theme.I211203"
            android:windowSoftInputMode="adjustResize" />

        <!-- Video Call Activity -->
        <activity
            android:name=".presentation.call.VideoCallActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.AppCompat.NoActionBar" />

        <!-- Profile Activities -->
        <activity
            android:name=".presentation.profile.EditProfileActivity"
            android:exported="false"
            android:theme="@style/Theme.I211203" />

        <activity
            android:name=".presentation.profile.UserProfileActivity"
            android:exported="false"
            android:theme="@style/Theme.I211203" />

        <!-- Follow Activities -->
        <activity
            android:name=".presentation.follow.FollowersActivity"
            android:exported="false"
            android:theme="@style/Theme.I211203" />

        <activity
            android:name=".presentation.follow.FollowingActivity"
            android:exported="false"
            android:theme="@style/Theme.I211203" />

        <!-- Story Activity -->
        <activity
            android:name=".presentation.story.StoryDisplayActivityNew"
            android:exported="false"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.AppCompat.NoActionBar" />

        <!-- Post Detail Activity -->
        <activity
            android:name=".presentation.post.PostDetailActivity"
            android:exported="false"
            android:theme="@style/Theme.I211203" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>
