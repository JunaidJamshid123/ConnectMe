<?xml version="1.0" encoding="utf-8"?>
<!-- Full-page Discover/Suggestions activity layout -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/white"
    android:fitsSystemWindows="true"
    android:orientation="vertical"
    tools:context=".presentation.discover.DiscoverPeopleActivity">

    <!-- Top bar -->
    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="48dp"
        android:background="@color/white"
        android:paddingHorizontal="16dp">

        <ImageView
            android:id="@+id/btn_back"
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:layout_alignParentStart="true"
            android:layout_centerVertical="true"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="Back"
            android:src="@drawable/ic_back_arrow"
            app:tint="#262626" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerInParent="true"
            android:fontFamily="sans-serif-medium"
            android:text="Discover people"
            android:textColor="#262626"
            android:textSize="16sp"
            android:textStyle="bold" />
    </RelativeLayout>

    <!-- Divider -->
    <View
        android:layout_width="match_parent"
        android:layout_height="0.5dp"
        android:background="#DBDBDB" />

    <!-- Shimmer loading -->
    <com.facebook.shimmer.ShimmerFrameLayout
        android:id="@+id/shimmer_suggestions"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:visibility="gone"
        app:shimmer_auto_start="true"
        app:shimmer_base_alpha="0.95"
        app:shimmer_highlight_alpha="0.55"
        app:shimmer_duration="1200">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:paddingTop="8dp">

            <!-- Shimmer rows -->
            <include layout="@layout/shimmer_suggestion_row" />
            <include layout="@layout/shimmer_suggestion_row" />
            <include layout="@layout/shimmer_suggestion_row" />
            <include layout="@layout/shimmer_suggestion_row" />
            <include layout="@layout/shimmer_suggestion_row" />
            <include layout="@layout/shimmer_suggestion_row" />
            <include layout="@layout/shimmer_suggestion_row" />
        </LinearLayout>
    </com.facebook.shimmer.ShimmerFrameLayout>

    <!-- Suggestions list -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recycler_suggestions"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:clipToPadding="false"
        android:overScrollMode="never"
        android:paddingTop="4dp"
        android:paddingBottom="16dp" />

    <!-- Empty state text -->
    <TextView
        android:id="@+id/empty_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:paddingTop="100dp"
        android:textColor="#8E8E8E"
        android:textSize="14sp"
        android:visibility="gone" />

</LinearLayout>
