<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:background="@color/white">

    <!-- ====== HEADER: profile pic, username/location/music, menu ====== -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:minHeight="52dp"
        android:paddingHorizontal="12dp"
        android:paddingVertical="8dp"
        android:gravity="center_vertical"
        android:orientation="horizontal">

        <de.hdodenhof.circleimageview.CircleImageView
            android:id="@+id/profile_image"
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:src="@drawable/default_profile"
            app:civ_border_width="0.5dp"
            app:civ_border_color="#EFEFEF" />

        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:layout_marginStart="10dp"
            android:orientation="vertical">

            <TextView
                android:id="@+id/username_text"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="username"
                android:textColor="#262626"
                android:textSize="13sp"
                android:fontFamily="sans-serif-medium"
                android:textStyle="bold" />

            <!-- Subtitle: location or music (alternates via code) -->
            <TextView
                android:id="@+id/subtitle_text"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:ellipsize="end"
                android:maxLines="1"
                android:textColor="#262626"
                android:textSize="11sp"
                android:visibility="gone" />
        </LinearLayout>

        <!-- "Follow" button next to menu (Instagram style) -->
        <TextView
            android:id="@+id/btn_follow_user"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginEnd="12dp"
            android:text="Follow"
            android:textColor="#0095F6"
            android:textSize="13sp"
            android:fontFamily="sans-serif-medium"
            android:textStyle="bold"
            android:clickable="true"
            android:focusable="true"
            android:background="?attr/selectableItemBackground"
            android:visibility="gone" />

        <ImageView
            android:id="@+id/menu_dots"
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:src="@drawable/ic_more_horiz"
            android:clickable="true"
            android:focusable="true"
            android:background="?attr/selectableItemBackgroundBorderless" />
    </LinearLayout>

    <!-- ====== POST IMAGE/VIDEO AREA ====== -->
    <FrameLayout
        android:id="@+id/post_image_container"
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <androidx.constraintlayout.widget.ConstraintLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content">

            <!-- Single image -->
            <ImageView
                android:id="@+id/post_image"
                android:layout_width="0dp"
                android:layout_height="0dp"
                android:scaleType="centerCrop"
                android:background="#FAFAFA"
                app:layout_constraintDimensionRatio="1:1"
                app:layout_constraintEnd_toEndOf="parent"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintTop_toTopOf="parent" />

            <!-- Carousel ViewPager2 -->
            <androidx.viewpager2.widget.ViewPager2
                android:id="@+id/post_carousel"
                android:layout_width="0dp"
                android:layout_height="0dp"
                android:visibility="gone"
                app:layout_constraintDimensionRatio="1:1"
                app:layout_constraintEnd_toEndOf="parent"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintTop_toTopOf="parent" />

            <!-- Video Player (ExoPlayer) -->
            <androidx.media3.ui.PlayerView
                android:id="@+id/video_player"
                android:layout_width="0dp"
                android:layout_height="0dp"
                android:visibility="gone"
                android:background="#000000"
                app:use_controller="false"
                app:surface_type="texture_view"
                app:resize_mode="zoom"
                app:layout_constraintDimensionRatio="1:1"
                app:layout_constraintEnd_toEndOf="parent"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintTop_toTopOf="parent" />

            <!-- Video Thumbnail (shown before play / as loading state) -->
            <ImageView
                android:id="@+id/video_thumbnail"
                android:layout_width="0dp"
                android:layout_height="0dp"
                android:scaleType="centerCrop"
                android:background="#FAFAFA"
                android:visibility="gone"
                app:layout_constraintDimensionRatio="1:1"
                app:layout_constraintEnd_toEndOf="parent"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintTop_toTopOf="parent" />

        </androidx.constraintlayout.widget.ConstraintLayout>

        <!-- Heart animation for double-tap like (Lottie) -->
        <com.airbnb.lottie.LottieAnimationView
            android:id="@+id/double_tap_heart"
            android:layout_width="150dp"
            android:layout_height="150dp"
            android:layout_gravity="center"
            android:visibility="gone"
            app:lottie_rawRes="@raw/like_heart_animation"
            app:lottie_autoPlay="false"
            app:lottie_loop="false" />

        <!-- Counter badge "1/3" (top-right, like Instagram) -->
        <TextView
            android:id="@+id/carousel_counter"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="top|end"
            android:layout_marginTop="16dp"
            android:layout_marginEnd="16dp"
            android:background="@drawable/bg_circle_dark"
            android:paddingHorizontal="9dp"
            android:paddingVertical="4dp"
            android:text="1/3"
            android:textColor="@color/white"
            android:textSize="12sp"
            android:fontFamily="sans-serif-medium"
            android:visibility="gone" />

        <!-- Video Progress Bar (bottom of video) -->
        <ProgressBar
            android:id="@+id/video_progress"
            style="@style/Widget.AppCompat.ProgressBar.Horizontal"
            android:layout_width="match_parent"
            android:layout_height="3dp"
            android:layout_gravity="bottom"
            android:max="100"
            android:progress="0"
            android:progressTint="#FFFFFF"
            android:progressBackgroundTint="#80FFFFFF"
            android:visibility="gone" />

        <!-- Mute/Unmute Icon (bottom-right) -->
        <ImageView
            android:id="@+id/mute_icon"
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:layout_gravity="bottom|end"
            android:layout_marginEnd="12dp"
            android:layout_marginBottom="12dp"
            android:padding="6dp"
            android:background="@drawable/bg_circle_dark"
            android:src="@drawable/ic_volume_off"
            android:visibility="gone"
            app:tint="@color/white" />

        <!-- Video Duration Badge (bottom-left when video) -->
        <TextView
            android:id="@+id/video_duration"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="bottom|start"
            android:layout_marginStart="12dp"
            android:layout_marginBottom="12dp"
            android:background="@drawable/bg_circle_dark"
            android:paddingHorizontal="8dp"
            android:paddingVertical="4dp"
            android:text="0:15"
            android:textColor="@color/white"
            android:textSize="11sp"
            android:fontFamily="sans-serif-medium"
            android:visibility="gone" />

        <!-- Video Views Count (top-left for reels/videos) -->
        <LinearLayout
            android:id="@+id/video_views_container"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="top|start"
            android:layout_marginStart="12dp"
            android:layout_marginTop="12dp"
            android:background="@drawable/bg_circle_dark"
            android:gravity="center_vertical"
            android:orientation="horizontal"
            android:paddingHorizontal="8dp"
            android:paddingVertical="4dp"
            android:visibility="gone">

            <ImageView
                android:layout_width="12dp"
                android:layout_height="12dp"
                android:src="@drawable/ic_visibility"
                app:tint="@color/white" />

            <TextView
                android:id="@+id/video_views_count"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginStart="4dp"
                android:text="0"
                android:textColor="@color/white"
                android:textSize="11sp"
                android:fontFamily="sans-serif-medium" />
        </LinearLayout>

        <!-- Play/Pause Center Button (for manual control) -->
        <ImageView
            android:id="@+id/play_pause_button"
            android:layout_width="60dp"
            android:layout_height="60dp"
            android:layout_gravity="center"
            android:alpha="0"
            android:src="@drawable/ic_play_circle"
            android:visibility="gone"
            app:tint="@color/white" />

    </FrameLayout>

    <!-- ====== ACTION BAR: heart, comment, share ... dots ... save ====== -->
    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="44dp"
        android:paddingHorizontal="12dp">

        <!-- Left action buttons -->
        <LinearLayout
            android:id="@+id/left_actions"
            android:layout_width="wrap_content"
            android:layout_height="match_parent"
            android:layout_alignParentStart="true"
            android:gravity="center_vertical"
            android:orientation="horizontal">

            <ImageView
                android:id="@+id/heart"
                android:layout_width="24dp"
                android:layout_height="24dp"
                android:src="@drawable/ic_heart_outline"
                android:clickable="true"
                android:focusable="true"
                android:background="?attr/selectableItemBackgroundBorderless" />

            <ImageView
                android:id="@+id/comment"
                android:layout_width="24dp"
                android:layout_height="24dp"
                android:layout_marginStart="16dp"
                android:src="@drawable/ic_comment"
                android:clickable="true"
                android:focusable="true"
                android:background="?attr/selectableItemBackgroundBorderless" />

            <ImageView
                android:id="@+id/send"
                android:layout_width="24dp"
                android:layout_height="24dp"
                android:layout_marginStart="16dp"
                android:src="@drawable/ic_share"
                android:clickable="true"
                android:focusable="true"
                android:background="?attr/selectableItemBackgroundBorderless" />
        </LinearLayout>

        <!-- Carousel dots (centered) -->
        <LinearLayout
            android:id="@+id/carousel_dots"
            android:layout_width="wrap_content"
            android:layout_height="match_parent"
            android:layout_centerInParent="true"
            android:gravity="center"
            android:orientation="horizontal"
            android:visibility="gone" />

        <!-- Save/Bookmark (right) -->
        <ImageView
            android:id="@+id/save"
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:layout_alignParentEnd="true"
            android:layout_centerVertical="true"
            android:src="@drawable/ic_bookmark"
            android:clickable="true"
            android:focusable="true"
            android:background="?attr/selectableItemBackgroundBorderless" />
    </RelativeLayout>

    <!-- ====== LIKES ROW (Instagram style with profile pics) ====== -->
    <LinearLayout
        android:id="@+id/likes_container"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center_vertical"
        android:orientation="horizontal"
        android:paddingHorizontal="14dp"
        android:paddingTop="2dp">

        <!-- Overlapping profile pics for likes (optional, shown via code) -->
        <FrameLayout
            android:id="@+id/liked_by_avatars"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:visibility="gone">

            <de.hdodenhof.circleimageview.CircleImageView
                android:id="@+id/liked_by_avatar_3"
                android:layout_width="18dp"
                android:layout_height="18dp"
                android:layout_marginStart="24dp"
                android:src="@drawable/default_profile"
                app:civ_border_width="1dp"
                app:civ_border_color="@color/white" />

            <de.hdodenhof.circleimageview.CircleImageView
                android:id="@+id/liked_by_avatar_2"
                android:layout_width="18dp"
                android:layout_height="18dp"
                android:layout_marginStart="12dp"
                android:src="@drawable/default_profile"
                app:civ_border_width="1dp"
                app:civ_border_color="@color/white" />

            <de.hdodenhof.circleimageview.CircleImageView
                android:id="@+id/liked_by_avatar_1"
                android:layout_width="18dp"
                android:layout_height="18dp"
                android:src="@drawable/default_profile"
                app:civ_border_width="1dp"
                app:civ_border_color="@color/white" />
        </FrameLayout>

        <!-- Likes count text -->
        <TextView
            android:id="@+id/likes_count"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="0 likes"
            android:textColor="#262626"
            android:textSize="13sp"
            android:fontFamily="sans-serif-medium"
            android:textStyle="bold" />
    </LinearLayout>

    <!-- ====== CAPTION ====== -->
    <TextView
        android:id="@+id/post_caption"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:lineSpacingExtra="3dp"
        android:paddingHorizontal="14dp"
        android:paddingTop="4dp"
        android:textColor="#262626"
        android:textSize="13sp" />

    <!-- ====== VIEW ALL COMMENTS ====== -->
    <TextView
        android:id="@+id/view_comments"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:paddingHorizontal="14dp"
        android:paddingTop="4dp"
        android:text="View all comments"
        android:textColor="#8E8E8E"
        android:textSize="13sp"
        android:background="?attr/selectableItemBackground" />

    <!-- ====== ADD COMMENT ROW ====== -->
    <LinearLayout
        android:id="@+id/add_comment_row"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center_vertical"
        android:orientation="horizontal"
        android:paddingHorizontal="14dp"
        android:paddingTop="6dp">

        <de.hdodenhof.circleimageview.CircleImageView
            android:id="@+id/add_comment_avatar"
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:src="@drawable/default_profile"
            app:civ_border_width="0dp" />

        <TextView
            android:id="@+id/add_comment_text"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="8dp"
            android:text="Add a comment..."
            android:textColor="#8E8E8E"
            android:textSize="13sp" />
    </LinearLayout>

    <!-- ====== TIMESTAMP ====== -->
    <TextView
        android:id="@+id/timestamp"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:letterSpacing="0.01"
        android:paddingHorizontal="14dp"
        android:paddingTop="6dp"
        android:paddingBottom="14dp"
        android:text="2 HOURS AGO"
        android:textAllCaps="true"
        android:textColor="#8E8E8E"
        android:textSize="10sp" />

</LinearLayout>